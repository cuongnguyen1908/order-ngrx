export class Order {
  // orderNumber: string;
  // phone: string;
  // address: string;
  // status: string;

  constructor(public orderCode: string, public phone : string, public address: string, public status: string) {}
}
